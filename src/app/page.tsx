import { Container, Text } from "@chakra-ui/react";
import type { FC } from "react";

const HomePage: FC = () => {
	return (
		<Container marginY="4">
			<Text>Home</Text>
		</Container>
	);
};

export default HomePage;
